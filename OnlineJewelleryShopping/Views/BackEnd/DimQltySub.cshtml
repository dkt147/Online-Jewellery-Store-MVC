@model IEnumerable<OnlineJewelleryShopping.Models.DimQltySubMst>

@{
    ViewBag.Title = "DimQltySub";
    Layout = "~/Views/Shared/_BackEnd.cshtml";
}

<h2>DimQltySub</h2>

<p>
    @Html.ActionLink("Create New", "CreateDimQltySub")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DimSubType_Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DimQlty)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DimSubType_Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DimQlty)
        </td>
        <td>
            <button class="btn btn-warning">@Html.ActionLink("Edit", "Edit", new { id = item.DimSubType_Id})</button>
            <button class="btn btn-danger"> @Html.ActionLink("Delete", "Delete", new { id = item.DimSubType_Id })</button>
        </td>
    </tr>
}

</table>
