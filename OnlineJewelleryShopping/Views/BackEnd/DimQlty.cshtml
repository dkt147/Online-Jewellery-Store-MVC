@model IEnumerable<OnlineJewelleryShopping.Models.DimQltyMst>

@{
    ViewBag.Title = "DimQlty";
    Layout = "~/Views/Shared/_BackEnd.cshtml";
}

<h2>DimQlty</h2>

<p>
    @Html.ActionLink("Create New", "CreateDimQlty")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DimQlty_Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DimQlty)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DimQlty_Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DimQlty)
        </td>
        <td>
            <button class="btn btn-warning">@Html.ActionLink("Edit", "Edit", new { id = item.DimQlty_Id})</button>
            <button class="btn btn-danger"> @Html.ActionLink("Delete", "Delete", new { id = item.DimQlty_Id })</button>
        </td>
    </tr>
}

</table>
