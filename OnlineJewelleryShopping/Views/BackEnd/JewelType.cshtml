@model IEnumerable<OnlineJewelleryShopping.Models.JewelTypeMst>

@{
    ViewBag.Title = "JewelType";
    Layout = "~/Views/Shared/_BackEnd.cshtml";
}

<h2>JewelType</h2>

<p>
    @Html.ActionLink("Create New", "CreateJewelType")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Jewellery_Type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Jewellery_Type)
        </td>
        <td>
            <button class="btn btn-warning">@Html.ActionLink("Edit", "Edit", new { id = item.Id})</button>
            <button class="btn btn-danger"> @Html.ActionLink("Delete", "Delete", new { id = item.Id})</button>
        </td>
    </tr>
}

</table>
